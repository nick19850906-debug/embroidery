<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI 기반 자수 자동 견적기</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; padding: 40px; background-color: #f4f7f6; }
       .container { max-width: 600px; margin: auto; background: white; padding: 30px; border-radius: 10px; box-shadow: 0 4px 8px rgba(0,0,0,0.1); }
        h1 { color: #333; text-align: center; }
       .upload-area { border: 2px dashed #4CAF50; padding: 20px; text-align: center; margin-bottom: 20px; cursor: pointer; border-radius: 8px;}
        button { background: #4CAF50; color: white; border: none; padding: 10px 20px; width: 100%; border-radius: 5px; font-size: 16px; cursor: pointer; }
        button:hover { background: #45a049; }
        #result { margin-top: 20px; padding: 15px; background: #eef; border-radius: 5px; white-space: pre-wrap; font-size: 14px; line-height: 1.6;}
        #loader { display: none; text-align: center; margin-top: 10px; font-weight: bold; color: #4CAF50;}
    </style>
</head>
<body>

<div class="container">
    <h1>🧵 AI 자수 견적기</h1>
    <form id="uploadForm">
        <div class="upload-area" onclick="document.getElementById('fileInput').click()">
            이곳을 클릭하여 도안 이미지(로고 등)를 업로드하세요.
        </div>
        <input type="file" id="fileInput" accept="image/*" style="display: none;" required>
        <button type="submit">견적 산출하기</button>
    </form>
    
    <div id="loader">OpenCV 및 Gemini AI로 분석 중입니다... 잠시만 기다려주세요.</div>
    <div id="result"></div>
</div>

<script>
    document.getElementById('uploadForm').addEventListener('submit', async (e) => {
        e.preventDefault();
        
        const fileInput = document.getElementById('fileInput');
        if (fileInput.files.length === 0) return alert('이미지를 선택해주세요.');

        const formData = new FormData();
        formData.append('file', fileInput.files);

        document.getElementById('loader').style.display = 'block';
        document.getElementById('result').innerText = '';

        try {
            // FastAPI 서버가 로컬(8000포트)에서 실행 중이라고 가정
            const response = await fetch('http://127.0.0.gov:8000/api/estimate', {
                method: 'POST',
                body: formData
            });
            
            const data = await response.json();
            document.getElementById('result').innerText = data.expert_quote;
        } catch (error) {
            document.getElementById('result').innerText = "서버 연결에 실패했습니다. 백엔드 서버가 켜져 있는지 확인하세요.";
        } finally {
            document.getElementById('loader').style.display = 'none';
        }
    });
</script>

</body>
</html>
fastapi
uvicorn
opencv-python-headless
google-genai
python-multipart